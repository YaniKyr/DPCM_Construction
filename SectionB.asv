load source.mat 

signal = t;

y_tide = my_quantizer(0.5,8,-3.5,3.5);
p=10;

quantized_sig = zeros(size(signal));

for i = 1:quantized_sig
    quantized_sig = my_quantizer(signal(i),8,3.5,3.5);
end


sig = zeros(1,length(signal));
for n =1:length(signal)
    if n>p
        for i =1:p
            y_tide = y_tide + a(i)* signal(n-i);
        end
    else
        y_tide = signal(n);

    end
end
